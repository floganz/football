$(document).ready(function(){
	// if(window.location.toString().indexOf('/users/new')!=-1)
	// {
	// 	$("form").on('click','input',function(event){
	// 		if (/^[A-Z0-9]+@[A-Z0-9]+\.[A-Z]{3,}$/.test($("#user_email").val())) {
	// 				// EMAIL VALIDATION
	// 		}
	// 		if($("#user_password").val()!=$("#user_password_conf").val())
	// 		{
	// 			console.log("ERROR")
	// 			event.preventDefault();
	// 		}
	// 	})
	// }
	//validation form
});